import React from 'react';

import { Modal } from './Modal';

export const HomePage = () => (
  <div className='main-container'>
    <Modal
      className='modal'
      color='blue'
    />
  </div>
);
